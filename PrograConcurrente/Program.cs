namespace PrograConcurrente
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // new Threads().Run();
            // new Tasks().Run();
            // new AsyncAwait().Run().Wait();
            // no correr -- new EfAsync().Run().Wait();
            // new ThreadPoolLimit().Run();
            //Pipes.RunServer();
            //Pipes.RunClient();
            // new Lock().Run().Wait();

            Console.WriteLine("Listo");
            Console.ReadLine();
        }
    }
}